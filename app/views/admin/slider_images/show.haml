= render :partial => "admin/left_hand_navigation", :locals => { :links => admin_links_for(@current_community) }
/ .left-navi-section
/   -if @sliderimage.avatar.present?
/     = image_tag(@sliderimage.avatar.url,:style=>"width:50%;")
/   -else
/     = image_tag("image-missing-md.png",:style=>"width:50%;")

/   = link_to 'Back', admin_slider_images_path
    
/   .left-navi-section
/     %a{:href => "#{@sliderimage.image_url}"}
/       %img{:src => @sliderimage.image_url, :title => ""}

.left-navi-section
  -if @sliderimage.avatar.present?
    %a{:href => "#{@sliderimage.image_url}"}
      %img{:src => @sliderimage.avatar,:style=>"width:100%;"}
  -else
    = image_tag("image-missing-md.png",:style=>"width:50%;")

  = link_to 'Back', admin_slider_images_path
